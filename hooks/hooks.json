{
  "$comment": [
    "Hooks are the TRIGGER layer. They are deterministic shell processes run by Claude Code",
    "itself -- they are not the agent, and they cannot reason. Keep them small, fast and",
    "fail-open: a hook that crashes must not break the session.",
    "",
    "Both hooks here are written in Node so they behave identically on Windows and POSIX.",
    "Both exit 0 on any internal error, on purpose."
  ],
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": ["node", "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/session-start.mjs"],
            "timeout": 15,
            "statusMessage": "Checking for in-progress testing state..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": ["node", "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/guard-destructive.mjs"],
            "timeout": 10,
            "statusMessage": "Checking command against the testing authorization policy..."
          }
        ]
      }
    ]
  }
}
